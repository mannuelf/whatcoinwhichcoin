/*!
 * @nuxt/cli v2.4.3 (c) 2016-2019

 * - All the amazing contributors
 * Released under the MIT License.
 * Website: https://nuxtjs.org
*/
'use strict';

require('path');
require('consola');
require('fs');
require('esm');
const __chunk_2 = require('./cli-chunk2.js');
require('@nuxt/config');
require('boxen');
require('chalk');
require('pretty-bytes');
require('std-env');
const __chunk_3 = require('./cli-chunk3.js');

const start = {
  name: 'start',
  description: 'Start the application in production mode (the application should be compiled with `nuxt build` first)',
  usage: 'start <dir>',
  options: {
    ...__chunk_3.common,
    ...__chunk_3.server
  },
  async run(cmd) {
    const config$$1 = await cmd.getNuxtConfig({ dev: false, _start: true });
    const nuxt = await cmd.getNuxt(config$$1);

    // Listen and show ready banner
    await nuxt.server.listen();
    __chunk_2.showBanner(nuxt);
  }
};

exports.default = start;
